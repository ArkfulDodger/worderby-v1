import React, { useState, useEffect } from "react";
import { View, Text } from "react-native";

const StartNewGameScreen = (props) => {
  return (
    <View>
      <Text>Start New Game</Text>
    </View>
  );
};

export default StartNewGameScreen;
